[project]
name = "ut2004packageutil"
version = "1.0.0"
description = "Unreal package parser/writer for UT2004"
requires-python = ">=3.12"


[project.optional-dependencies]
dev = [
    'mypy',
    'pre-commit',
    'pytest',
    'ruff',
]
